/**
 * 로비 주소 ( /lobby ) — 미션을 고르는 화면이 열리는 자리.
 *
 * 실제 내용은 components/LobbyScreen.tsx 에 있습니다.
 */

import { LobbyScreen } from "@/components/LobbyScreen";

export default function LobbyPage() {
  return <LobbyScreen />;
}
