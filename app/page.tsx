/**
 * 앱의 첫 주소 ( / ) — 시작 화면이 열리는 자리.
 *
 * 실제 내용은 components/StartScreen.tsx 에 있습니다.
 */

import { StartScreen } from "@/components/StartScreen";

export default function StartPage() {
  return <StartScreen />;
}
