# 골든타임 — 재디자인 안내서 (코덱스용)

이 문서는 **디자인을 다시 하는 AI(코덱스)** 를 위한 안내서입니다.

앱은 기능이 전부 완성되어 동작합니다. 당신이 할 일은 **보이는 것만** 다시 만드는 것입니다.

---

## 0. 30초 요약

| | |
|---|---|
| **무엇** | 초등 3학년~중학생 진로체험 수업용 웹앱. 간호 현장 세 곳을 체험 |
| **누가 씀** | 학생이 태블릿으로. 교실에서 30명이 동시에 |
| **당신이 할 일** | `components/`, `app/`, `styles/` 의 **생김새만** 다시 만들기 |
| **절대 하면 안 되는 일** | `data/`, `lib/` 폴더 수정 |
| **끝나고 반드시** | `npm run build` 와 `npm run test:data` 가 통과하는지 확인 |

---

## 1. 건드려도 되는 파일 / 절대 안 되는 파일

### ⛔ 절대 수정 금지

```
data/     ← 정답, 기준값, 환자 카드, 통과 코드, 문구
lib/      ← 채점, 타이머, BPM 계산, 배지·유형 판정
scripts/  ← 데이터 점검 스크립트
```

이 폴더에는 **수업의 정답과 기준**이 들어 있습니다.
디자인을 바꾸려고 여기를 건드리면 수업 내용이 망가집니다.
값이 필요하면 **가져다 쓰기만** 하세요.

### ✅ 마음껏 바꿔도 됨

```
styles/design-tokens.css   ← 색·글자 크기·간격. 여기부터 시작하세요
app/globals.css            ← 공통 스타일 (⚠️ 인쇄 규칙은 아래 5번 참고)
app/**/page.tsx            ← 주소 연결 (거의 손댈 일 없음)
components/*.tsx           ← 화면 전부
components/*.module.css    ← 화면 스타일 전부
```

---

## 2. 화면 파일 목록과 하는 일

### 공통

| 파일 | 하는 일 |
|---|---|
| `SessionProvider.tsx` | 학생 진행 상황 보관. **생김새 없음. 손대지 마세요** |
| `AppScreen.tsx` | 모든 화면의 공통 틀 (제목 띠 + 소리 버튼 + 본문) |
| `PrimaryButton.tsx` | 큰 버튼 |
| `MissionRouter.tsx` | 미션 주소 → 화면 연결. **생김새 없음** |

### 화면

| 파일 | 화면 | 감정 톤 |
|---|---|---|
| `StartScreen.tsx` | 시작 — 이름 입력, 난이도 선택, 면책 문구 | 차분하고 설레는. 병원 문 앞 |
| `LobbyScreen.tsx` + `HospitalFloorPlan.tsx` | 로비 — **위에서 내려다본 병원 조감도** | 도착한 설렘. 어디 갈지 고르는 기분 |
| `TriageMissionScreen.tsx` | 미션1 응급실 | **긴박함.** 환자가 밀려들고 시간이 없음 |
| `TriagePatientCard.tsx` | 미션1 — 옮기는 환자 카드 | 〃 |
| `TriageZoneDropArea.tsx` | 미션1 — 색깔 구역 | 〃 |
| `ExplanationCard.tsx` | 미션1 — 오답 즉시 해설 (팝업) | 알려주는 느낌. 혼내지 않기 |
| `MissionTimer.tsx` | 제한시간 시계 | 〃 |
| `AmbulanceMissionScreen.tsx` | 미션2 119 | **집중.** 리듬감, 손끝에 힘 |
| `DispatchOrderStep.tsx` | 미션2 ① 신고 순서 | 〃 |
| `CompressionPad.tsx` | 미션2 ② 가슴압박 (리듬 원) | 〃 |
| `BpmMeter.tsx` | 미션2 ② 실시간 속도계 | 〃 |
| `AedBodyDiagram.tsx` | 미션2 ③ AED 패드 위치 | 〃 |
| `HealthRoomMissionScreen.tsx` | 미션3 보건실 | **따뜻함.** 안심되는, 부드러운 |
| `HealthRoomCaseCard.tsx` | 미션3 — 상황과 선택지 | 〃 |
| `HealthLogBuilder.tsx` | 미션3 — 보건일지 (중등만) | 차분한 마무리 |
| `UnlockCodeScreen.tsx` | 통과 코드 입력 | 숨 고르는 대기실 |
| `FinalReport.tsx` | 최종 리포트 | **축하.** 뿌듯한 마무리 |
| `Certificate.tsx` | 수료증 (인쇄됨) | 기념하는 느낌 |
| `MissionScoreChart.tsx` | 미션별 점수 그래프 | 〃 |

각 파일 **맨 위 주석**에 그 화면이 뭘 하는지, 어떤 데이터를 받는지 한국어로 적혀 있습니다. 먼저 읽어보세요.

---

## 3. 절대 바꾸면 안 되는 값

이 값들은 전부 `data/rules.ts` 에 모여 있습니다.
**화면 코드에 이 숫자를 직접 쓰지 마세요.** 함수를 불러 쓰세요.

| 값 | 내용 |
|---|---|
| CPR 속도 기준 | **분당 100~120회** |
| 통과 기준 | **초등 60% / 중등 75%** |
| 통과 코드 | **미션1 `1004` / 미션2 `2119` / 미션3 `3365`** |
| 환자 카드 정답 배치 | `data/triage-patients.ts` |
| 의료 면책 문구 | `data/disclaimer.ts` — **글자를 바꾸지 마세요** |

```ts
❌ if (bpm >= 100 && bpm <= 120)     ✅ if (isGoodRhythm(bpm))
❌ if (accuracy >= 60)               ✅ if (isPassing(tally, difficulty))
❌ if (code === "1004")              ✅ if (isValidUnlockCode("er", code))
```

### 난이도별로 다른 것 (기획서 확정 사항)

| 항목 | 초등 | 중등 |
|---|---|---|
| 미션1 구역 수 | 4개 | 5개 (주황 "몇 분 안에" 추가) |
| 미션1 환자 수 | 6명 | 10명 |
| 미션1 활력징후 | 안 보임 | 보임 |
| 미션1 제한시간 | 없음 | 3분 |
| 미션2 압박 시간 | 60초 | 120초 |
| 미션2 리듬 가이드 원 | 끝까지 | 20초 뒤 사라짐 |
| 미션3 보건일지 | 없음 | 있음 |

**배지 3개를 다 모아야 최종 리포트가 열립니다.**

---

## 4. 대상이 초등 3학년~중학생입니다

### 크기

`styles/design-tokens.css` 에 최소값이 정해져 있습니다. **이보다 작게 만들지 마세요.**

```css
--touch-target-min: 56px;     /* 버튼 최소 높이 */
--touch-target-large: 76px;   /* 주요 버튼 */
--drop-zone-min-height: 180px;/* 카드 놓는 구역 */
```

- 태블릿 **터치가 우선**입니다. 마우스로도 되어야 합니다.
- 손가락은 정확하지 않습니다. 누르는 곳을 넉넉하게.
- 글자는 어른용 앱보다 한 단계 크게.
- 가로(1024×768)와 세로(768×1024) **둘 다** 확인하세요.

### 그림

- **사실적인 피·상처 이미지 금지.** 일러스트나 아이콘만 씁니다.
- 환자 카드와 AED 몸 그림은 **단순한 실루엣**으로.
- 각 데이터 파일에 `illustrationHint` 가 있습니다. 그림 고를 때 참고하세요.

### 색

- 중증도 구역 색은 **의미**가 있습니다: 급한 순서로 빨강 → 주황 → 노랑 → 초록 → 파랑.
  명도·채도를 다듬는 건 괜찮지만 순서가 뒤집히면 안 됩니다.
- **색만으로 알려주지 마세요.** 색맹 학생을 위해 글자도 함께 남기세요.
  (구역 이름, BPM 상태 "좋아요/느려요", 점수 % 등)

### 두 가지 조작 방법을 남겨주세요

- **미션1**: 카드를 끌어다 놓기 **+ 구역을 그냥 누르기**
- **미션2 ①**: 순서대로 한 장씩 누르기 (드래그 방식으로 바꾸지 마세요)

드래그를 못 하는 학생이 반드시 있습니다. 누르기 방식을 없애면 그 학생은 수업을 못 합니다.

---

## 5. 건드리면 깨지는 것들

### 인쇄 (수료증)

`app/globals.css` 맨 아래 `@media print` 규칙과 두 이름표:

- `print-area` — 인쇄에 나올 부분 (`Certificate.tsx`)
- `no-print` — 인쇄에서 뺄 부분 (버튼, 그래프 등)

이 이름표를 지우면 인쇄가 깨집니다. 수료증은 **A4 한 장**에 들어가야 합니다.

### 가슴압박 성능

`CompressionPad.tsx` 의 리듬 원은 **React 를 거치지 않고** `ref` 로 직접 움직입니다.
교실에서 태블릿 30대가 동시에 돌기 때문입니다.
원을 React 상태로 매 프레임 다시 그리게 바꾸면 느려집니다.

또 `.guideCircle` 의 `transform` 에 **CSS transition 을 걸지 마세요.** 리듬이 어긋납니다.

### 터치 중 스크롤

`touch-action: none` 이 걸린 곳이 있습니다 (환자 카드, 압박 패드).
지우면 태블릿에서 드래그·연타할 때 화면이 같이 스크롤됩니다.

### 조감도 좌표

`HospitalFloorPlan.tsx` 의 `ROOM_LAYOUT` 은 그림(SVG)과 버튼이 **함께 보는 좌표표**입니다.
방을 옮기려면 이 숫자만 바꾸세요. 그림과 버튼이 같이 움직입니다.

### 초점 표시

`globals.css` 의 `:focus-visible` 은 키보드로 조작하는 학생을 위한 것입니다. 지우지 마세요.

---

## 6. 로비를 조감도로 만들어 두었습니다

기획서 권장에 따라 로비는 **부서 카드 나열이 아니라 위에서 내려다본 병원 평면도**입니다.

지금은 단순한 형태입니다. **더 실감나게 만들어도 좋습니다.**
(복도, 대기 의자, 화단, 접수 창구, 구급차가 드나드는 진입로 등)

구조는 두 겹입니다:

1. **뒤** — SVG 배경 (건물 바닥, 복도, 진입로). 누를 수 없음
2. **앞** — 진짜 `<button>` 세 개 (부서). 키보드로도 눌림

SVG 안에 글자를 넣지 마세요. 그림이 늘어날 때 글자가 찌그러집니다.
글자는 앞쪽 HTML 에 두세요.

---

## 7. 다 바꾼 뒤 확인

```bash
npm run build      # 빌드가 되는지
npm run test:data  # 데이터와 계산이 안 깨졌는지
npm run dev        # 직접 눌러보기
```

`npm run test:data` 는 정답 개수, 구역 수, 통과 코드, CPR 기준 등을 자동으로 확인하고
**문제가 있으면 한국어로 알려줍니다.**

### 직접 눌러볼 것

- [ ] 시작 → 로비 → 미션 → 통과 코드 → 로비 → 리포트가 이어지는지
- [ ] 미션1에서 **드래그와 누르기 둘 다** 되는지
- [ ] 미션2 리듬 원이 매끄럽게 움직이고 BPM 숫자가 뜨는지
- [ ] 미션3에서 ①을 고른 뒤 ②가 나타나는지
- [ ] 리포트에서 **인쇄 시 수료증만** 나오는지
- [ ] 가로·세로 태블릿 모두에서 잘리는 곳이 없는지
- [ ] 면책 문구가 시작 화면과 리포트에 그대로 있는지

---

## 8. 앱 구조 한눈에

```
시작 화면 ──▶ 로비(병원 조감도) ──▶ 미션 ──▶ 통과 코드 ──▶ 로비
                     │                                      │
                     └──── 배지 3개를 다 모으면 ──▶ 최종 리포트 + 수료증
```

- 로그인 없음. **새로고침하면 처음부터** (학교 개인정보 승인 절차를 피하기 위한 의도적 설계)
- 학생 이름은 수료증 표시용이며 **어디에도 저장·전송되지 않습니다**
- 소리는 **기본 꺼짐** (교실에서 30대가 동시에 울리면 안 되므로)
- 인터넷이 끊겨도 동작합니다 (모든 화면을 미리 만들어 둠)
